━━━ 人工验证请求 ━━━━

步骤: 电源电压检测
文件: ela_pow_det_drv.c/h, ela_pow_det_usr.c/h, elaco_main.c

验证内容:
□ 功能完整性    ─── ADC 读取 POW_MT6816(PA0) 电压值，串口每秒打印一次
□ 边界条件      ─── ADC 转换失败时返回 0 不崩溃；PA0 开路/短路时读数合理
□ 硬件对齐      ─── 引脚 PA0 (ADC1_IN0) 配置正确，无引脚冲突
□ 编译结果      ─── 0 error, 0 warning

验证方法:
1. 编译项目，确认 0 error 0 warning
2. 烧录固件到目标板
3. 打开串口 (USART3, 115200)，观察每秒输出 `[POW_DET] ADC: xxxx, Voltage: x.xxx V`
4. 测量 PA0 实际电压，与串口读数对比确认公式正确

预期结果:
- 编译通过，无错误无警告
- 串口每秒输出一行电压值
- 读数与万用表测量值偏差 < 5%

━━━━━━━━━━━━━━━━
